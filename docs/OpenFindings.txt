Phase 7:
    - Tank in any slot in hotbar. Select slot with item in it, tank is darker. Select slot with no item in it (empy hand), tank is normal brightness (INVENTORY ONLY). GL issues maybe if latter?
    - Tank's glass does not render alpha properly now when empty in inventory

Phase 10:
    - Status upgrades unusable

Miscellaneous:
    - Controller should be able to take fluid container as item instead of fluid if storage drawer storing it is closer than fluid tank
    - GL leaks
    - Tanks overlays don't show in WAILA
